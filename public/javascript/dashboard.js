
// Add button selectors for adding and editing a post
// addPost = (event) => {
//     event.preventDefault();

//     // Grab values from the form

//     // Post a fetch request to the database

//     // Reload the page

// }

// Add listeners for buttons
// document.querySelector("#new-post-form").addEventListener("submit", addPost(event));